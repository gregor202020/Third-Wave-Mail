---
phase: 07-code-quality-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/vitest.config.ts
  - packages/api/tests/sns-idempotency.unit.test.ts
  - packages/api/tests/bulk-send-dedup.unit.test.ts
  - packages/api/tests/segment-logic.unit.test.ts
  - packages/api/src/routes/webhooks-inbound.ts
  - packages/workers/src/workers/bulk-send.worker.ts
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06

must_haves:
  truths:
    - "Vitest produces passing tests for SNS handler idempotency"
    - "Vitest produces passing tests for bulk-send deduplication"
    - "Vitest produces passing tests for segment query AND/OR logic"
    - "Vitest coverage report is generated for critical paths"
  artifacts:
    - path: "packages/api/vitest.config.ts"
      provides: "Vitest configuration with V8 coverage"
      contains: "coverage"
    - path: "packages/api/tests/sns-idempotency.unit.test.ts"
      provides: "Unit tests for SNS bounce/complaint idempotency"
      contains: "numInsertedOrUpdatedRows"
    - path: "packages/api/tests/bulk-send-dedup.unit.test.ts"
      provides: "Unit tests for bulk-send duplicate message check"
      contains: "shouldSkipSend"
    - path: "packages/api/tests/segment-logic.unit.test.ts"
      provides: "Unit tests for segment AND/OR rule SQL generation"
      contains: "buildRuleFilter"
  key_links:
    - from: "packages/api/tests/sns-idempotency.unit.test.ts"
      to: "packages/api/src/routes/webhooks-inbound.ts"
      via: "imports processBounceSnsEvent"
      pattern: "processBounceSnsEvent"
    - from: "packages/api/tests/bulk-send-dedup.unit.test.ts"
      to: "packages/workers/src/workers/bulk-send.worker.ts"
      via: "imports shouldSkipSend"
      pattern: "shouldSkipSend"
    - from: "packages/api/tests/segment-logic.unit.test.ts"
      to: "packages/shared/src/segments.ts"
      via: "imports buildRuleFilter and buildSingleRule"
      pattern: "buildRuleFilter"
---

<objective>
Configure Vitest with V8 coverage and write unit tests for the three critical code paths: SNS handler idempotency, bulk-send deduplication, and segment AND/OR query logic.

Purpose: These three areas had bugs in prior phases. Unit tests catch regressions without requiring a running database or full integration test setup.
Output: vitest.config.ts, three unit test files, extracted testable helper functions.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-code-quality-tooling/07-RESEARCH.md

<interfaces>
<!-- Key functions and types the executor needs -->

From packages/api/src/routes/webhooks-inbound.ts:
- `webhooksInboundRoutes: FastifyPluginAsync` — the SNS handler plugin
- SNS idempotency uses: `db.insertInto('events').values({...}).onConflict((oc) => oc.column('sns_message_id').doNothing()).executeTakeFirst()`
- The `numInsertedOrUpdatedRows === 0n` check determines duplicate (skip counter increment)
- Need to EXTRACT a helper function for unit testing

From packages/workers/src/workers/bulk-send.worker.ts:
- `createBulkSendWorker(): Worker` — BullMQ worker factory
- Dedup check: `db.selectFrom('messages').select('id').where('campaign_id', '=', ...).where('contact_id', '=', ...).executeTakeFirst()`
- If existing message found, the contact is skipped
- Need to EXTRACT a helper function for unit testing

From packages/shared/src/segments.ts:
- `buildRuleFilter(group: SegmentRuleGroup)` — returns Kysely expression builder callback
- `buildSingleRule(eb, rule: SegmentRule)` — builds individual rule filter
- `resolveSegmentContactIds(segmentId: number): Promise<number[]>` — full resolution
- SegmentRuleGroup has `logic: 'and' | 'or'` and `rules: (SegmentRule | SegmentRuleGroup)[]`
- Already exported and testable. Use Kysely `compile()` to assert generated SQL.

From packages/shared/src/types.ts:
- `SegmentRule` type, `SegmentRuleGroup` type
- `EventType`, `ContactStatus`, `MessageStatus` constants
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure Vitest and extract testable helpers from SNS handler and bulk-send worker</name>
  <files>packages/api/vitest.config.ts, packages/api/src/routes/webhooks-inbound.ts, packages/workers/src/workers/bulk-send.worker.ts</files>
  <behavior>
    - vitest.config.ts: Vitest resolves and runs tests in packages/api/tests/
    - SNS helper: processBounceSnsEvent(db, snsMessageId, messageId, email, bounceType) returns { inserted: boolean }
    - Dedup helper: shouldSkipSend(db, campaignId, contactId) returns boolean
  </behavior>
  <action>
1. Install @vitest/coverage-v8 in api package:
   ```
   npm install --save-dev @vitest/coverage-v8 --workspace=packages/api
   ```

2. Create `packages/api/vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';

   export default defineConfig({
     test: {
       environment: 'node',
       globals: false,
       include: ['tests/**/*.test.ts'],
       exclude: ['tests/setup.ts'],
       coverage: {
         provider: 'v8',
         include: ['src/**/*.ts'],
         exclude: ['src/index.ts', 'src/seed-*.ts'],
         reporter: ['text', 'lcov'],
       },
     },
   });
   ```
   Note: Do NOT set globals: true (existing tests import from 'vitest' explicitly). Do NOT set coverage thresholds yet — baseline first, enforce later.

3. Extract `processBounceSnsEvent` from `packages/api/src/routes/webhooks-inbound.ts`:
   - Find the code block that inserts a bounce/complaint event with `onConflict(...).doNothing()`
   - Extract it into an exported async function: `export async function processBounceSnsEvent(db: Kysely<Database>, snsMessageId: string, messageId: string, email: string, bounceType: string): Promise<{ inserted: boolean }>`
   - The function should do: insert event row with the given fields, ON CONFLICT (sns_message_id) DO NOTHING, return `{ inserted: (result?.numInsertedOrUpdatedRows ?? 0n) > 0n }`
   - Replace the inline code in the route handler with a call to this function
   - The function must accept `db` as a parameter (not call `getDb()` internally) so tests can pass a mock

4. Extract `shouldSkipSend` from `packages/workers/src/workers/bulk-send.worker.ts`:
   - Find the code block that checks `db.selectFrom('messages').select('id').where('campaign_id'...).where('contact_id'...).executeTakeFirst()`
   - Extract it into an exported async function: `export async function shouldSkipSend(db: Kysely<Database>, campaignId: number, contactId: number): Promise<boolean>`
   - Returns `true` if existing message found, `false` otherwise
   - Replace the inline code in the worker with a call to this function
   - Accept `db` as parameter for testability

5. Verify existing tests still pass: `cd packages/api && npx vitest run`
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail/packages/api && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>vitest.config.ts exists. processBounceSnsEvent and shouldSkipSend exported as standalone functions. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for SNS idempotency, bulk-send dedup, and segment logic</name>
  <files>packages/api/tests/sns-idempotency.unit.test.ts, packages/api/tests/bulk-send-dedup.unit.test.ts, packages/api/tests/segment-logic.unit.test.ts</files>
  <behavior>
    - SNS test 1: First SNS notification inserts event (inserted=true)
    - SNS test 2: Duplicate SNS notification returns inserted=false (ON CONFLICT DO NOTHING)
    - Dedup test 1: No existing message for campaign+contact returns shouldSkipSend=false
    - Dedup test 2: Existing message record returns shouldSkipSend=true
    - Segment test 1: Single AND group with 2 rules produces SQL with AND operator
    - Segment test 2: Single OR group with 2 rules produces SQL with OR operator
    - Segment test 3: Nested AND inside OR produces correct parenthesization
    - Segment test 4: within_days operator produces >= (now - N*86400000ms) comparison
    - Segment test 5: between operator produces >= low AND <= high comparison
  </behavior>
  <action>
1. Create `packages/api/tests/sns-idempotency.unit.test.ts`:
   - Import `processBounceSnsEvent` from the webhooks-inbound route
   - Use `vi.fn()` to create a mock Kysely db object with chainable methods: `insertInto().values().onConflict().executeTakeFirst()`
   - Test case: "first notification inserts successfully" — mock returns `{ numInsertedOrUpdatedRows: 1n }` — assert `inserted === true`
   - Test case: "duplicate notification skips" — mock returns `{ numInsertedOrUpdatedRows: 0n }` — assert `inserted === false`
   - Test case: "null result treated as not inserted" — mock returns `undefined` — assert `inserted === false`

2. Create `packages/api/tests/bulk-send-dedup.unit.test.ts`:
   - Import `shouldSkipSend` from bulk-send.worker.ts
   - Use `vi.fn()` to create a mock Kysely db object with chainable methods: `selectFrom().select().where().where().executeTakeFirst()`
   - Test case: "no existing message returns false" — mock returns `undefined` — assert result is `false`
   - Test case: "existing message returns true" — mock returns `{ id: 42 }` — assert result is `true`

3. Create `packages/api/tests/segment-logic.unit.test.ts`:
   - Import `buildRuleFilter` and `buildSingleRule` from `@twmail/shared`
   - For SQL assertion, create a minimal Kysely instance with a dummy dialect (no real DB connection) and use `.compile()` to get the generated SQL string
   - Alternative simpler approach: use `getDb()` from shared if a DATABASE_URL is available in test env, or create a Kysely instance with `PostgresDialect` and `{ connectionString: 'postgres://localhost/test' }` just for SQL compilation (no actual connection needed for compile())
   - Test case: AND group — build filter with `{ logic: 'and', rules: [{field: 'status', operator: 'equals', value: 'active'}, {field: 'country', operator: 'equals', value: 'US'}] }` — compiled SQL contains `AND`
   - Test case: OR group — same with `logic: 'or'` — compiled SQL contains `OR`
   - Test case: Nested — OR group containing AND subgroup — compiled SQL has correct parenthesization
   - Test case: within_days operator — rule with `{ field: 'last_open_at', operator: 'within_days', value: 30 }` — compiled SQL has `>=` comparison
   - Test case: between operator — rule with `{ field: 'engagement_score', operator: 'between', value: [10, 50] }` — compiled SQL has both bounds

   IMPORTANT: The segment tests should test the SQL STRUCTURE, not execute queries. Use Kysely's `.compile()` method to get the SQL string and assert on it. This requires creating a Kysely instance but does NOT require a running database.

4. Run all unit tests: `cd packages/api && npx vitest run`
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail/packages/api && npx vitest run tests/sns-idempotency.unit.test.ts tests/bulk-send-dedup.unit.test.ts tests/segment-logic.unit.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All three unit test files exist and pass. SNS idempotency covers first-insert and duplicate cases. Bulk-send dedup covers skip and proceed cases. Segment logic covers AND, OR, nested groups, within_days, and between operators via compiled SQL assertion.</done>
</task>

</tasks>

<verification>
1. `cd packages/api && npx vitest run` — all tests pass (existing + new)
2. `cd packages/api && npx vitest run --coverage` — coverage report generated
3. Three new test files exist in packages/api/tests/
4. processBounceSnsEvent is exported from webhooks-inbound.ts
5. shouldSkipSend is exported from bulk-send.worker.ts
</verification>

<success_criteria>
- Vitest configured with V8 coverage for packages/api
- SNS idempotency tested: duplicate SNS messages return inserted=false
- Bulk-send dedup tested: existing message record causes skip
- Segment AND/OR logic tested: compiled SQL has correct structure
- All existing integration tests continue to pass
- No database required to run the new unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-quality-tooling/07-02-SUMMARY.md`
</output>
