---
phase: 12-production-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-ses-dns.ts
  - scripts/check-health.sh
  - packages/api/tests/production-readiness.test.ts
  - .env.example
autonomous: true
requirements:
  - OBS-04
  - OBS-05

must_haves:
  truths:
    - "A verification script checks SES domain identity, SPF, DKIM, and DMARC records programmatically"
    - "The DMARC policy is at minimum p=quarantine"
    - "A health check script can verify the /health endpoint returns 200 with database and redis checks"
    - ".env.example documents all production-required environment variables"
  artifacts:
    - path: "scripts/verify-ses-dns.ts"
      provides: "SES DNS verification script"
      contains: "GetEmailIdentityCommand"
    - path: "scripts/check-health.sh"
      provides: "Health endpoint verification script"
      contains: "/health"
    - path: "packages/api/tests/production-readiness.test.ts"
      provides: "Source-code scan tests verifying production readiness"
  key_links:
    - from: "scripts/verify-ses-dns.ts"
      to: "@aws-sdk/client-sesv2"
      via: "GetEmailIdentityCommand"
      pattern: "GetEmailIdentityCommand"
    - from: "packages/api/src/routes/health.ts"
      to: "@twmail/shared"
      via: "getDb, getRedis"
      pattern: "getDb.*getRedis"
---

<objective>
Create production launch verification tooling: a DNS verification script for SES domain identity (SPF, DKIM, DMARC), a health endpoint check script, and source-code scan tests confirming production readiness.

Purpose: Before going live, Greg needs to verify that DNS records are correct and the health endpoint is externally reachable. These scripts automate the manual checks.
Output: verify-ses-dns.ts script, check-health.sh script, production-readiness tests, updated .env.example.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-launch/12-RESEARCH.md

@packages/api/src/routes/health.ts
@packages/workers/src/ses-client.ts
@.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SES DNS verification script + production readiness tests</name>
  <files>
    scripts/verify-ses-dns.ts,
    packages/api/tests/production-readiness.test.ts,
    .env.example
  </files>
  <behavior>
    - Test 1 (source-code scan): scripts/verify-ses-dns.ts exists and contains GetEmailIdentityCommand
    - Test 2 (source-code scan): scripts/verify-ses-dns.ts checks SPF via dns.resolveTxt
    - Test 3 (source-code scan): scripts/verify-ses-dns.ts checks DMARC and verifies p=quarantine or p=reject
    - Test 4 (source-code scan): .env.example contains SES_SENDING_DOMAIN placeholder
    - Test 5 (source-code scan): packages/api/src/routes/health.ts checks both database and redis connectivity
    - Test 6 (source-code scan): scripts/check-health.sh exists and curls the /health endpoint
  </behavior>
  <action>
    1. Create `scripts/verify-ses-dns.ts` — standalone TypeScript script (run with tsx) that:
       - Takes domain from SES_SENDING_DOMAIN env var or first CLI arg (default: thirdwavebbq.com.au)
       - Checks SES domain identity status via GetEmailIdentityCommand (DkimAttributes.Status, VerifiedForSendingStatus)
       - Checks SPF via dns.promises.resolveTxt — looks for `include:amazonses.com`
       - Checks DKIM via SES API response (DkimAttributes.Tokens → verifies CNAME records exist)
       - Checks DMARC via dns.promises.resolveTxt on `_dmarc.{domain}` — verifies p=quarantine or p=reject
       - Prints pass/fail for each check with color output
       - Exits 0 if all pass, 1 if any fail
       - Import SESv2Client from @aws-sdk/client-sesv2 (already a dependency in workers package)

    2. Create `scripts/check-health.sh` — simple bash script that:
       - Takes BASE_URL from env or first arg (default: http://localhost:3000)
       - Curls $BASE_URL/health with 10s timeout
       - Checks HTTP status is 200
       - Parses JSON response to verify database: ok and redis: ok
       - Prints clear pass/fail output
       - Exits 0/1 appropriately

    3. Update `.env.example` — add:
       - SES_SENDING_DOMAIN=thirdwavebbq.com.au (for DNS verification script)
       - UPTIME_MONITOR_URL= (placeholder for external monitoring webhook/dashboard)

    4. Create `packages/api/tests/production-readiness.test.ts` — source-code scan tests:
       - Verify scripts/verify-ses-dns.ts exists and contains required patterns
       - Verify scripts/check-health.sh exists and contains curl /health
       - Verify health.ts checks both database and redis
       - Verify .env.example contains SES_SENDING_DOMAIN

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && npx vitest run tests/production-readiness.test.ts</automated>
  </verify>
  <done>
    - scripts/verify-ses-dns.ts checks SES identity, SPF, DKIM, DMARC programmatically
    - scripts/check-health.sh curls /health and validates response
    - .env.example has SES_SENDING_DOMAIN and UPTIME_MONITOR_URL
    - All source-code scan tests pass
  </done>
</task>

</tasks>

<verification>
1. `cd packages/api && npx vitest run tests/production-readiness.test.ts` — all pass
2. `npx tsx scripts/verify-ses-dns.ts --help` or `npx tsx scripts/verify-ses-dns.ts` — runs without crash (may fail DNS checks in dev, that's expected)
3. `bash scripts/check-health.sh http://localhost:3000` — runs without crash
</verification>

<success_criteria>
- SES DNS verification script exists and checks all 4 DNS requirements (identity, SPF, DKIM, DMARC)
- DMARC check enforces minimum p=quarantine policy
- Health check script validates /health endpoint response
- .env.example documents production-required variables
- Source-code scan tests verify all artifacts exist
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-launch/12-01-SUMMARY.md`
</output>
