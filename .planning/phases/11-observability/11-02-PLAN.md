---
phase: 11-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/package.json
  - packages/frontend/src/instrumentation.ts
  - packages/frontend/src/instrumentation-client.ts
  - packages/frontend/src/sentry.server.config.ts
  - packages/frontend/src/sentry.edge.config.ts
  - packages/frontend/src/app/global-error.tsx
  - packages/frontend/next.config.ts
  - packages/frontend/Dockerfile
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "An unhandled exception in the Next.js frontend (client or server) creates a Sentry event"
    - "React rendering errors are caught by the global error boundary and reported to Sentry"
    - "Server Component and middleware errors are captured via the onRequestError hook"
  artifacts:
    - path: "packages/frontend/src/instrumentation.ts"
      provides: "Next.js server-side Sentry init + onRequestError hook"
      exports: ["register", "onRequestError"]
    - path: "packages/frontend/src/instrumentation-client.ts"
      provides: "Client-side Sentry init"
      contains: "Sentry.init"
    - path: "packages/frontend/src/app/global-error.tsx"
      provides: "React error boundary for App Router"
      contains: "captureException"
    - path: "packages/frontend/next.config.ts"
      provides: "Next config wrapped with withSentryConfig"
      contains: "withSentryConfig"
  key_links:
    - from: "packages/frontend/next.config.ts"
      to: "@sentry/nextjs"
      via: "withSentryConfig wrapper"
      pattern: "withSentryConfig"
    - from: "packages/frontend/src/instrumentation.ts"
      to: "packages/frontend/src/sentry.server.config.ts"
      via: "dynamic import in register()"
      pattern: "import.*sentry\\.server"
---

<objective>
Add Sentry error tracking to the Next.js frontend for client-side, server-side, and edge runtime error capture.

Purpose: Frontend errors must be visible in Sentry alongside API and worker errors. Next.js 16.1.6 with App Router requires specific instrumentation files and a global error boundary.
Output: @sentry/nextjs installed, instrumentation hooks for server and client, global-error.tsx boundary, next.config.ts wrapped with withSentryConfig.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-observability/11-RESEARCH.md

@packages/frontend/next.config.ts
@packages/frontend/package.json
@packages/frontend/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @sentry/nextjs and create instrumentation files</name>
  <files>
    packages/frontend/package.json,
    packages/frontend/src/instrumentation.ts,
    packages/frontend/src/instrumentation-client.ts,
    packages/frontend/src/sentry.server.config.ts,
    packages/frontend/src/sentry.edge.config.ts,
    packages/frontend/src/app/global-error.tsx,
    packages/frontend/next.config.ts,
    packages/frontend/Dockerfile
  </files>
  <action>
    1. Install @sentry/nextjs:
       `npm install @sentry/nextjs --workspace=packages/frontend`

    2. Create `packages/frontend/src/sentry.server.config.ts`:
       - Import * as Sentry from '@sentry/nextjs'
       - Call Sentry.init with dsn from process.env.SENTRY_DSN, tracesSampleRate: 1.0, sendDefaultPii: false, debug: false

    3. Create `packages/frontend/src/sentry.edge.config.ts`:
       - Same as server config but for edge runtime

    4. Create `packages/frontend/src/instrumentation.ts`:
       - Export async function register() that dynamically imports sentry.server.config when NEXT_RUNTIME === 'nodejs' and sentry.edge.config when NEXT_RUNTIME === 'edge'
       - Import Sentry from @sentry/nextjs and export onRequestError = Sentry.captureRequestError (for Next.js 15+ server error capture)

    5. Create `packages/frontend/src/instrumentation-client.ts`:
       - Import * as Sentry from '@sentry/nextjs'
       - Call Sentry.init with dsn from process.env.NEXT_PUBLIC_SENTRY_DSN, tracesSampleRate: 1.0, debug: false

    6. Create `packages/frontend/src/app/global-error.tsx`:
       - 'use client' directive
       - Import * as Sentry from '@sentry/nextjs'
       - Import useEffect from 'react'
       - Default export a component that takes { error, reset } props
       - In useEffect, call Sentry.captureException(error)
       - Render a simple error UI with "Something went wrong" heading and a "Try again" button that calls reset()

    7. Update `packages/frontend/next.config.ts`:
       - Import { withSentryConfig } from '@sentry/nextjs'
       - Wrap the export: `export default withSentryConfig(nextConfig, { silent: true })`

    8. Update `packages/frontend/Dockerfile` (if needed):
       - Add NEXT_PUBLIC_SENTRY_DSN as ARG and ENV so it's baked into the client bundle at build time
       - Add SENTRY_DSN as ENV for server-side runtime

    9. Update `docker-compose.yml` — add NEXT_PUBLIC_SENTRY_DSN and SENTRY_DSN to the frontend service environment block (empty defaults).
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && npm run build --workspace=packages/frontend 2>&1 | tail -5</automated>
  </verify>
  <done>
    - @sentry/nextjs installed in frontend package
    - instrumentation.ts exports register() and onRequestError
    - instrumentation-client.ts initializes Sentry for browser
    - global-error.tsx catches React rendering errors and reports to Sentry
    - next.config.ts wrapped with withSentryConfig
    - Frontend builds successfully with `npm run build`
  </done>
</task>

</tasks>

<verification>
1. `npm run build --workspace=packages/frontend` — builds without errors
2. `grep -r "Sentry.init" packages/frontend/src/` — found in instrumentation-client.ts and sentry.server.config.ts
3. `grep "withSentryConfig" packages/frontend/next.config.ts` — found
4. `grep "captureException" packages/frontend/src/app/global-error.tsx` — found
5. `grep "onRequestError" packages/frontend/src/instrumentation.ts` — found
</verification>

<success_criteria>
- @sentry/nextjs installed and next.config.ts wrapped
- Server, edge, and client instrumentation files created
- Global error boundary catches and reports React errors
- Frontend builds cleanly
- NEXT_PUBLIC_SENTRY_DSN env var documented in docker-compose
</success_criteria>

<output>
After completion, create `.planning/phases/11-observability/11-02-SUMMARY.md`
</output>
