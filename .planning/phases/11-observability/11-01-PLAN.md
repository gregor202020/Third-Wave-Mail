---
phase: 11-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/instrument.mjs
  - packages/api/src/app.ts
  - packages/api/src/config.ts
  - packages/api/package.json
  - packages/api/Dockerfile
  - packages/workers/src/instrument.mjs
  - packages/workers/src/logger.ts
  - packages/workers/src/index.ts
  - packages/workers/src/workers/bulk-send.worker.ts
  - packages/workers/src/workers/import.worker.ts
  - packages/workers/src/workers/webhook.worker.ts
  - packages/workers/src/workers/resend.worker.ts
  - packages/workers/src/workers/ab-eval.worker.ts
  - packages/workers/src/scheduler.ts
  - packages/workers/package.json
  - packages/workers/Dockerfile
  - docker-compose.yml
  - packages/api/tests/sentry-init.unit.test.ts
  - packages/api/tests/pino-redact.unit.test.ts
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03

must_haves:
  truths:
    - "An unhandled exception in the API creates a Sentry event with context"
    - "An unhandled exception in any worker creates a Sentry event with context"
    - "Production log output is structured JSON with email addresses and JWT tokens redacted"
    - "pino-pretty is absent from all production bundles and docker images"
    - "Workers produce structured JSON logs in production, not console.log text"
  artifacts:
    - path: "packages/api/src/instrument.mjs"
      provides: "Sentry ESM init for API"
      contains: "Sentry.init"
    - path: "packages/workers/src/instrument.mjs"
      provides: "Sentry ESM init for workers"
      contains: "Sentry.init"
    - path: "packages/workers/src/logger.ts"
      provides: "Pino logger instance for workers"
      exports: ["logger"]
    - path: "packages/api/tests/sentry-init.unit.test.ts"
      provides: "Source-code scan verifying Sentry wiring"
    - path: "packages/api/tests/pino-redact.unit.test.ts"
      provides: "Source-code scan verifying redact paths and pino-pretty absence"
  key_links:
    - from: "packages/api/Dockerfile"
      to: "packages/api/src/instrument.mjs"
      via: "CMD node --import ./src/instrument.mjs"
      pattern: "--import.*instrument"
    - from: "packages/workers/Dockerfile"
      to: "packages/workers/src/instrument.mjs"
      via: "CMD node --import ./src/instrument.mjs"
      pattern: "--import.*instrument"
    - from: "packages/api/src/app.ts"
      to: "@sentry/node"
      via: "setupFastifyErrorHandler"
      pattern: "setupFastifyErrorHandler"
    - from: "packages/workers/src/index.ts"
      to: "packages/workers/src/logger.ts"
      via: "import { logger }"
      pattern: "import.*logger"
---

<objective>
Add Sentry error tracking and structured Pino logging with PII redaction to the API and workers packages.

Purpose: Production errors must be visible in Sentry without exposing user PII in log output. Workers currently use console.log which produces unstructured text — must be replaced with Pino for JSON output.
Output: instrument.mjs files for Sentry ESM init in both packages, Pino redact config on API logger, standalone Pino logger for workers replacing all console.log calls, Dockerfile CMD updates for --import flag, source-code scan tests verifying wiring.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-observability/11-RESEARCH.md

@packages/api/src/app.ts
@packages/api/src/config.ts
@packages/api/package.json
@packages/api/Dockerfile
@packages/workers/src/index.ts
@packages/workers/package.json
@packages/workers/Dockerfile
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sentry init + Fastify error handler + Pino redact config + worker logger</name>
  <files>
    packages/api/src/instrument.mjs,
    packages/api/src/app.ts,
    packages/api/src/config.ts,
    packages/api/package.json,
    packages/api/Dockerfile,
    packages/workers/src/instrument.mjs,
    packages/workers/src/logger.ts,
    packages/workers/package.json,
    packages/workers/Dockerfile,
    docker-compose.yml
  </files>
  <behavior>
    - Test 1 (source-code scan): packages/api/src/instrument.mjs exists and contains Sentry.init with dsn from process.env.SENTRY_DSN and sendDefaultPii: false
    - Test 2 (source-code scan): packages/api/src/app.ts contains setupFastifyErrorHandler call
    - Test 3 (source-code scan): packages/api/src/app.ts logger config contains redact.paths with req.headers.authorization, req.headers.cookie, req.body.email, req.body.password
    - Test 4 (source-code scan): packages/workers/src/instrument.mjs exists and contains Sentry.init
    - Test 5 (source-code scan): packages/workers/src/logger.ts exists with redact config and pino-pretty guarded by NODE_ENV !== 'production'
    - Test 6 (source-code scan): packages/api/src/app.ts does NOT reference pino-pretty in the production logger path
    - Test 7 (source-code scan): packages/workers/src/logger.ts does NOT use pino-pretty in the production path
  </behavior>
  <action>
    1. Install @sentry/node in api and workers packages:
       `npm install @sentry/node --workspace=packages/api --workspace=packages/workers`

    2. Install pino in workers package (Fastify already bundles it for API):
       `npm install pino --workspace=packages/workers`

    3. Add pino-pretty as devDependency in both packages for dev convenience:
       `npm install pino-pretty --save-dev --workspace=packages/api --workspace=packages/workers`

    4. Create `packages/api/src/instrument.mjs` — plain .mjs file (NOT TypeScript), imports @sentry/node, calls Sentry.init with dsn from process.env.SENTRY_DSN, environment from process.env.NODE_ENV, tracesSampleRate: 1.0, sendDefaultPii: false.

    5. Create `packages/workers/src/instrument.mjs` — same pattern as API instrument.mjs.

    6. Update `packages/api/src/config.ts` — add SENTRY_DSN: z.string().optional() to the envSchema.

    7. Update `packages/api/src/app.ts`:
       - Import * as Sentry from '@sentry/node'
       - Extract logger config into a helper function `buildLogger(config: Config)` that:
         - Defines redact paths: ['req.headers.authorization', 'req.headers.cookie', 'req.body.email', 'req.body.password', '*.email']
         - In non-production: returns { level, redact, transport: { target: 'pino-pretty', options: { translateTime: 'HH:MM:ss Z', ignore: 'pid,hostname' } } }
         - In production: returns { level, redact } (NO pino-pretty reference)
       - After creating the Fastify instance, call Sentry.setupFastifyErrorHandler(app) BEFORE other plugin registrations
       - Log warning at startup if NODE_ENV=production and SENTRY_DSN is not set: app.log.warn('SENTRY_DSN not configured — Sentry is disabled in production')

    8. Create `packages/workers/src/logger.ts`:
       - Import pino from 'pino'
       - Export a `logger` instance with level from LOG_LEVEL env (default 'info')
       - Redact paths: ['*.email', '*.authorization']
       - Conditional pino-pretty transport guarded by NODE_ENV !== 'production'

    9. Update `packages/api/Dockerfile` — change CMD from:
       `CMD ["node", "packages/api/dist/index.js"]`
       to:
       `CMD ["node", "--import", "./packages/api/src/instrument.mjs", "packages/api/dist/index.js"]`
       Note: instrument.mjs is plain JS, not compiled — it must be available as source in the Docker image. Add a COPY line for it if the multi-stage build doesn't already include src/.

    10. Update `packages/workers/Dockerfile` — same pattern:
        `CMD ["node", "--import", "./packages/workers/src/instrument.mjs", "packages/workers/dist/index.js"]`

    11. Update `docker-compose.yml` — add SENTRY_DSN env var (empty default) to api and worker services.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && npm test --workspace=packages/api -- --reporter=verbose tests/sentry-init.unit.test.ts tests/pino-redact.unit.test.ts</automated>
  </verify>
  <done>
    - instrument.mjs exists in both api and workers with Sentry.init
    - app.ts has setupFastifyErrorHandler and pino redact config
    - workers/src/logger.ts exports structured pino logger with redact
    - Dockerfiles use --import for Sentry ESM loading
    - docker-compose has SENTRY_DSN env var
    - pino-pretty only in devDependencies and guarded by NODE_ENV check
    - All source-code scan tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all console.log/error/warn in workers with pino logger</name>
  <files>
    packages/workers/src/index.ts,
    packages/workers/src/workers/bulk-send.worker.ts,
    packages/workers/src/workers/import.worker.ts,
    packages/workers/src/workers/webhook.worker.ts,
    packages/workers/src/workers/resend.worker.ts,
    packages/workers/src/workers/ab-eval.worker.ts,
    packages/workers/src/scheduler.ts
  </files>
  <action>
    Replace every console.log, console.error, and console.warn call in the workers package with the pino logger from `./logger.ts` (or relative path).

    Import pattern: `import { logger } from '../logger.js';` (or `./logger.js` depending on depth)

    Mapping:
    - console.log(msg) -> logger.info(msg) or logger.info({ context }, msg) when structured data available
    - console.error(msg, err) -> logger.error({ err }, msg) — pino serializes Error objects via the `err` key
    - console.warn(msg) -> logger.warn(msg)

    For each file:
    - packages/workers/src/index.ts: Replace all 5 console.log/error calls with logger.info/error. Add `import { logger } from './logger.js';`
    - packages/workers/src/workers/bulk-send.worker.ts: Replace console.warn/error with logger.warn/error
    - packages/workers/src/workers/import.worker.ts: Replace console calls with logger
    - packages/workers/src/workers/webhook.worker.ts: Replace console calls with logger
    - packages/workers/src/workers/resend.worker.ts: Replace console calls with logger
    - packages/workers/src/workers/ab-eval.worker.ts: Replace console calls with logger
    - packages/workers/src/scheduler.ts: Replace console calls with logger

    Do NOT change console.warn calls in the API package — those use request.log (Fastify's built-in pino) per Phase 04-01 decision.
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && grep -rn "console\.\(log\|error\|warn\)" packages/workers/src/ && echo "FAIL: console calls remain" || echo "PASS: no console calls in workers"</automated>
  </verify>
  <done>
    - Zero console.log/error/warn calls remain in packages/workers/src/
    - All replaced with logger.info/error/warn from the pino logger
    - Workers package compiles with `npm run build --workspace=packages/workers`
  </done>
</task>

</tasks>

<verification>
1. `npm test --workspace=packages/api -- tests/sentry-init.unit.test.ts tests/pino-redact.unit.test.ts` — all pass
2. `grep -rn "console\.\(log\|error\|warn\)" packages/workers/src/` — returns no matches
3. `npm run build --workspace=packages/api` — compiles clean
4. `npm run build --workspace=packages/workers` — compiles clean
5. `grep "pino-pretty" packages/api/package.json packages/workers/package.json` — only in devDependencies
</verification>

<success_criteria>
- Sentry instrument.mjs files exist in api and workers packages
- Fastify error handler registered via setupFastifyErrorHandler
- Pino redact config strips email, password, authorization, cookie from logs
- Workers use pino logger — zero console.log/error/warn calls remain
- pino-pretty is devDependency only and guarded by NODE_ENV !== 'production'
- Dockerfiles use --import flag for Sentry ESM init
- All source-code scan tests green
</success_criteria>

<output>
After completion, create `.planning/phases/11-observability/11-01-SUMMARY.md`
</output>
